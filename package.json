{
  "name": "pay-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Shared payments toolkit — charges as typed components with per-component commission (tips can be 0%), instant (destination-charge) and escrow (capture→hold→release) money flows, merchant/customer subscriptions, Stripe adapter and webhook routing. Pure money math, storage- and client-agnostic.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "npm install --include-workspace-root -w @paykit/core -w @paykit/stripe -w @paykit/express && npm run build",
    "build": "npm run build -w @paykit/core -w @paykit/stripe -w @paykit/express",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.0"
  }
}
